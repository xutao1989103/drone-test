sudo: required

language: java

jdk:
  - oraclejdk8

services:
  - docker

after_success:
  - docker build -t muqi/cd .
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
      docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
      docker push xtaodocker/cd;
      fi